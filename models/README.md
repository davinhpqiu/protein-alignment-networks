# Pretrained model artefacts

Downloaded model weights and TensorFlow Hub caches live here and are not
committed. The pretrained DEDAL model is cached under `models/dedal`; see
`docs/DEDAL.md` for provenance and setup.
